/**
 * @file 43. Multiply Strings
 * @author arlenyang
 */

/**
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
function multiply(num1, num2) {
    let split = numStr => {
        let len = numStr.length;
        if (len === 1) {
            return {
                L: +numStr,
                R: 0,
                lP: 0,
            }
        }
    }   
}