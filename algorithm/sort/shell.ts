/**
 * @file implementation of shell sort
 * @author YDSS
 */

function shell(arr: number[]): number[] {
    let len = arr.length;
    let increment = Math.ceil(len / 2);
    
    for (; increment > 0; increment = Math.ceil(increment / 2)) {
    }
}