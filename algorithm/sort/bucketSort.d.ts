/**
 * @file bucket sort
 * @author arlenyang
 */
/**
 * T(n) = O(m + n), n is arr.length, m is bucket size
 * S(n) = O(m)
 * @param arr
 */
declare function bucketSort(arr: number[]): number[];
declare let arr: number[];
